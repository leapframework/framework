defaults:
  #http://www.h2database.com/html/functions.html
  functions:
    timestamp_add_millis: 'TIMESTAMPADD(MS, :interval, :timestamp)'
    timestamp_add_seconds: 'TIMESTAMPADD(S, :interval, :timestamp)'